<div class="jumbo">
  <div class="right tomster"></div>
  <h2>Welcome!</h2>
  {{component 'welcome-foo' sayFoo="FOO"}}
  <p>Enjoy your stay</p>
  {{#link-to 'about' class="button"}}
    About Us
  {{/link-to}}
</div>

{{#each model as |rentalUnit|}}
  {{rental-listing rental=rentalUnit}}
{{/each}}
{{outlet}}
